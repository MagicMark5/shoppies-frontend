export default function MovieResults(props) {

  
  
  if (Array.isArray(props.movieList)) {
    console.log("valid input")
  } else {
    console.log("invalid input")
  }


  return (
    <section>
      
    </section>
  )
}