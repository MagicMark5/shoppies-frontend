export const nominateMovie = (movie) => {
  // adds a movie to the Nominations movie list from the MovieResults
}

export const removeNomination = (movie) => {
  // removes a movie from the Nominations movie list

}

export const listMovieResults = (movieData) => {
  // Makes an <ul> in MovieResults with desired movie data from API response
  
}